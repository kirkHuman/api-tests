{
  "name": "api-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:desktop": "playwright test --project=desktop",
    "test:auth": "playwright test tests/desktop/auth",
    "test:users": "playwright test tests/desktop/users",
    "test:products": "playwright test tests/desktop/products",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.7.2"
  }
}
